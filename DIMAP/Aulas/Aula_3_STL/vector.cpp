#include <bits/stdc++.h>
	using namespace std;

	int main (){
	
    vector<int> v1;

    vector<int> v2(5, 10);

    vector<int> v3(v2);
	
    vector<int> v4 = {4, 7, 3, 2};
    
    //v.back -> último
    //v.front -> primeiro
    //push_back -> adiciona no final
    //pop_back -> retira o último
    //sort(v.begin(), v.end()); - what is v.begin()?

    //iterator to data_struc that dont iterate through the index (not the case of vector)


	

	return 0;
	}
