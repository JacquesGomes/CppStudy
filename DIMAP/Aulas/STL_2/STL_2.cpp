#include <bits/stdc++.h>

	using namespace std;
	int main (){

		//set = if the number is already in the set, he is
		//ignored and not inserted

		//multiset supports other numbers

		//lower_bound
		//upper_bound

		//upper_bound de 20 -> o próximo maior de vinte

		//o map é implementado com uma árvore balanceada

		map<string, int> matricula;

		matricula["jacques"] = 200031;

		cout << matricula["jacques"] << endl; // retorna o valor

		//set -> estrutura em árvore (rubro-negra)

		
	
	return 0;
	}
