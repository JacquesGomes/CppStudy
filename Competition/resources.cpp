#include <bits/stdc++.h>
//contain iostream, algorithms, vector...

//compile in terminal with: "g++ -std=c++11 -02 -Wall test.cpp -o test"

//Padrão c+11, -02 to optimize the code, wall to show warnings

//When working with files and input, write:
//freopen("input.txt", "r", stdin);
//freopne("output.txt", "w", stdout);
//it will read from input and print the output on the file, the rest works normal

using namespace std;

string methods (int total){
    
    string teste1;
    string teste2;
    string teste3;

    getline(cin, teste1); //Gets hole line

    cin >> teste2; //gets individual words, separated by a space or newline

    while(cin >> teste3){
        //used when the amount of data is unknown
    }


}

int main (){

    return 0;
}
